# =================================================================
# AI HR INTERVIEWER — DEPLOYMENT REQUIREMENTS
# =================================================================
# Used by Render (backend) and Streamlit Cloud (frontend)
# Does NOT include ollama (local only — replaced by Groq on cloud)
# =================================================================

# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36

# Validation
pydantic==2.10.3
pydantic-settings==2.6.1

# PDF Extraction
pypdf==5.1.0

# HTTP Client (Vapi API + Groq API)
httpx==0.27.2

# Excel Export
openpyxl==3.1.5

# Frontend
streamlit==1.41.1
pandas==2.2.3
requests==2.32.3

# Environment
python-dotenv==1.0.1

# Groq SDK (optional — we use httpx directly, but install anyway)
groq==0.11.0